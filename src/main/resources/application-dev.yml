server:
  servlet:
    context-path: /demo
  port: 9999

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: false
    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/retail_store_youtube
    username: postgres
    password: h123
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher